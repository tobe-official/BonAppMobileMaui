<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:BonAppMobileMaui.screens"
       x:Class="BonAppMobileMaui.AppShell"
       Shell.TabBarBackgroundColor="#F3F3E0">

    <ShellContent 
        Title="Login" 
        ContentTemplate="{DataTemplate local:LoginPage}" 
        Route="LoginPage" />

    <TabBar>
        <Tab>
            <ShellContent 
                Title=""
                Icon="home1.png"
                ContentTemplate="{DataTemplate local:HomePage}" 
                Route="HomePage" >
            </ShellContent>
        </Tab>

        <Tab>
            <ShellContent 
                Title="Add Post" 
                Icon="add1.png"
                ContentTemplate="{DataTemplate local:AddPost}" 
                Route="AddPostPage" />
        </Tab>

        <Tab>
            <ShellContent 
                Title="Saved Meals" 
                Icon="bookmark1.png"
                ContentTemplate="{DataTemplate local:SavedMealsPage}" 
                Route="SavedMealsPage" />
        </Tab>

        <Tab>
            <ShellContent 
                Title="Account" 
                Icon="account1.png"
                ContentTemplate="{DataTemplate local:AccountPage}" 
                Route="AccountPage">
            </ShellContent>
        </Tab>
    </TabBar>
</Shell>