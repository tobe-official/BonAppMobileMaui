<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BonAppMobileMaui.imageConverter"
             x:Class="BonAppMobileMaui.GlobalWidgets.MealListProfileFavorite">

    <ContentView.Resources>
        <!-- Converter für das Bild -->
        <local:ImageSourceConverter x:Key="ImageSourceConverter" />
    </ContentView.Resources>

    <CollectionView x:Name="MealCollectionView"
                    ItemsLayout="VerticalGrid, 3"
                    VerticalScrollBarVisibility="Never">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame BorderColor="Gray" Padding="0" CornerRadius="0" HeightRequest="100" WidthRequest="100">
                    <Image Source="{Binding ., Converter={StaticResource ImageSourceConverter}}" 
                           HeightRequest="100" WidthRequest="100" />
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentView>